# CF-Workers-SUB-Next — 当前状态与后续计划

> 最后更新：2026-08-16
> 本文档是项目实际状态的"单一事实来源"，取代或补充 17_ROADMAP.md（原始规划，未反映当前进度）。

---

## ✅ 已完成（可部署）

### 核心链路
| 模块 | 状态 | 说明 |
|------|:---:|------|
| 管理员登录 | ✅ | PBKDF2 密码 + HttpOnly Session Cookie，10次/分限流 |
| 订阅管理 | ✅ | 添加 / 删除 / 手动更新 |
| 节点解析 | ✅ | VMess / VLESS / Trojan / Shadowsocks 四协议 |
| 节点去重 | ✅ | 指纹去重 + 启用/禁用持久化 |
| Mihomo 配置生成 | ✅ | 完整 YAML，含 proxy-groups + rules |
| Sing-box / 其他格式 | ⚠️ 部分 | 见"待办" |
| 自动部署 CI | ✅ | GitHub Actions → Cloudflare Workers |

### 分流规则系统（近两天新增）
| 功能 | 状态 |
|------|:---:|
| 规则页：10 大类 86 规则 | ✅ |
| 勾选规则写入 Mihomo 配置（rule-providers + 有序 rules） | ✅ |
| rule-providers URL（jsDelivr CDN，已验证 86 分类全部存在） | ✅ |
| 规则库扫描（设置页搜索 1546 全量分类） | ✅ |
| 自定义规则加入（分组/标签/策略） | ✅ |
| 规则选择持久化（KV selected_rules + custom_rules） | ✅ |

### 界面与品牌
| 功能 | 状态 |
|------|:---:|
| 5 大页面（仪表盘/订阅/节点/规则/输出 + 设置） | ✅ |
| 仪表盘增强（启用数/禁用数/协议分布） | ✅ |
| 登录页 + 页脚品牌信息（GitHub/作者/版本） | ✅ |
| 升级检测（GitHub Releases 对比） | ✅ |
| 暗色/亮色主题 | ✅ |
| 节点启用保存（全量，不受搜索过滤影响） | ✅ |

---

## 🚧 待办清单（按优先级）

### P0 — 必要修正（建议尽快）
- [ ] **勾选规则写进 Mihomo 后真实验证**：部署后实际下载一份 Mihomo 配置，导入客户端验证分流是否生效（不是 100% 确定 86 条规则的 rule-providers 在 Mihomo 里都能正常拉取）
- [ ] **输出页面订阅链接**:Mihomo 的 `/sub/mihomo/:token` 订阅端点是否真实可用（节点实时更新）
- [ ] **KV 持久化验证**：部署后刷新/重启是否记住规则选择、节点启用、自定义规则

### P1 — 高价值（下一开发档）
- [ ] **Sing-box 规则支持**：Mihomo 规则已成型，Sing-box 用户可受益（rule-set 机制不同需单独实现）
- [ ] **Surge 规则支持**：Apple 生态用户
- [ ] **Dashboards更多维度**：今日更新节点、失败订阅告警
- [ ] **节点测速**（Ping/TCPing/URL）：⚠️ CF Worker 无任意 TCP 出站，实现受限；可能需客户端侧或 Durable Object 方案

### P2 — 增强
- [ ] **订阅历史/回滚**：保存历史版本，出错可回滚
- [ ] **模板系统**：用户自定义 Mihomo 模板
- [ ] **多管理员支持**：需重设计权限模型

### P3 — 开源生态（长期）
- [ ] 插件系统（扩展 parser）
- [ ] 社区模板共享
- [ ] 项目官网/文档站

---

## 🔴 明确不做（Roadmap 已列）
- 内置 VPN / 代理客户端（非项目目标）
- 商业化订阅平台

---

## 🔍 已知技术限制
| 限制 | 影响 | 后续思路 |
|------|------|---------|
| CF Worker 无任意 TCP 出站 | 节点 Ping/TCPing 测速做不了 | 客户端侧测速 / Durable Object / 第三方 API |
| CF Worker 5MB CPU 限制 | 大订阅（万级节点）解析可能超时 | 分批/流式解析优化 |
| KV 100MB 限制 | 海量历史版本不可行 | 定期清理 / R2 归档 |
| 免费计划每分钟子请求限制 | 大规模并发出站受限 | 订阅更新错峰 |

---

## 🧰 开发环境速查

```bash
npm install            # 安装依赖
npm run dev            # 本地 dev（wrangler dev --local）
npm test               # 运行 245 项测试
npm run lint           # ESLint
npm run build          # 类型检查 + 前端构建（内嵌 html）
npm run deploy         # 构建 + 部署 CF
```

当前测试覆盖：**245 项**（单元 + 集成 + 生成器 + 数据层），全部通过。

---

## 📋 提交历史（近两天）

| 提交 | 内容 |
|------|------|
| `c6ac711` | 规则库：设置页搜索 1546 分类 + 自定义加入 + 合并生成 |
| `143e10f` | 登录页+页脚品牌信息与升级检测 |
| `e9f7fcd` | 完善 README |
| `cd46762` | 仪表盘增强（启用/禁用/协议分布） |
| `ae5e55f` | 修复节点启用保存（全量数据） |
| `981c784` | 输出页订阅链接失败提示 |